## Automated tests

Run from repo root:

- `python social_media_api/manage.py test posts -v 2`

Current test suite:

- `posts.tests.PostsCommentsAPITests` (3 tests)

Expected result:

- All tests pass (`OK`).

